apiVersion: v2                  # Specifies Helm Chart API version (v2 is the current standard)
name: backend                   # The name of your Helm chart (usually the app or service name)
description: A Helm chart for Kubernetes  # A short description of what this chart is for
type: application              # Type of chart - 'application' means it's a deployable app (not a library)
version: 0.1.0                 # Version of this Helm chart (use semantic versioning: MAJOR.MINOR.PATCH)
appVersion: "1.16.0"           # Version of the actual application this chart deploys (e.g. your backend app version)